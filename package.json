{
  "name": "ovp-provider-cloudflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reference Cloudflare provider for Open Vehicle Passport (Workers + KV + Pages/assets).",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
